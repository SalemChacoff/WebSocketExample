spring:
  application:
    name: WebSocketExample

  data:
    mongodb:
      username: admin
      password: password
      host: localhost
      port: 27017
      database: chat_app
      authentication-database: admin

server:
  port: 8080
